apiVersion: v1
kind: ServiceAccount
metadata:
  name: my-app
  # No RBAC roles are bound by default. The app has no API server access.
  # If the app later needs to read ConfigMaps or Secrets, create a Role
  # and RoleBinding in the overlay — do not grant cluster-scoped access.
automountServiceAccountToken: false
