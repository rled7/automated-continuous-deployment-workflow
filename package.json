{
  "name": "automated-continuous-deployment-workflow",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-wide dev tooling only. The actual application lives in app/.",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.js": ["eslint --fix"],
    "*.{json,md,yml,yaml}": ["prettier --write"]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.4.2"
  }
}
